title: $:/plugins/byper/code-copy-button/readme
type: text/vnd.tiddlywiki

! 代码复制按钮插件

为 TiddlyWiki 的代码块自动添加复制按钮。

!! 功能特性

* 为独立代码块添加复制按钮
* 为行内代码添加复制按钮（鼠标悬停显示）
* 复制成功后显示反馈（绿色对勾）
* 使用 SVG 图标，美观清晰
* 响应式设计，适配不同主题
* 自动监听新渲染的代码块

!! 使用方法

安装插件后，所有代码块会自动获得复制功能。

!!! 独立代码块示例

```javascript
function hello() {
  console.log("Hello World");
}
```

代码块右上角会显示复制按钮图标。

!!! 行内代码示例

在文本中使用 `console.log()` 这样的行内代码，鼠标悬停时会在代码后显示复制按钮。

!! 自定义样式

你可以在自定义样式中覆盖按钮外观：

```css
.tc-copy-button {
  background: your-color;
}
```

!! 兼容性

* 需要 TiddlyWiki 5.1.22 或更高版本
* 需要现代浏览器支持 Clipboard API
* 自动降级到传统复制方法

!! 版本历史

|!版本|!日期|!更新内容|
|1.0.0|2025-11-04|初始版本，支持独立代码块和行内代码块复制|

!! 作者

byp

!! 许可

MIT License
